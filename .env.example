# ------------------------------------------------------------------
# MySQL Database credentials
# Copy this file to .env.local for local development, or add the
# same keys under Vercel -> Project -> Settings -> Environment Variables
# ------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_NAME=library_management
DB_USER=root
DB_PASSWORD=min#stb123@2007

# ------------------------------------------------------------------
# Auth
# Secret used to sign/verify JWT session cookies.
# Generate a strong random string for production, e.g.:
#   openssl rand -base64 48
# ------------------------------------------------------------------
JWT_SECRET=libraryms-system-minha_secure_2026
